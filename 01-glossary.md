# 名词详解

不只是词典。每个条目尽量回答三件事：**是什么 / 它看不到什么 / 常被误用成什么**。

最后一节「常见混淆」是本文的重点——大部分沟通成本花在这些地方。

---

## 一、扫描类型

四个字母缩写经常被混着用，但它们**看的东西完全不同**，互相替代不了。

### SAST（Static Application Security Testing，静态应用安全测试）

读**源码**找缺陷，不运行程序。典型：Semgrep、SonarQube、Checkmarx。

- **看得到**：注入拼接、硬编码逻辑、不安全的 API 用法、代码模式
- **看不到**：运行时才成立的问题（配置、环境变量、部署拓扑）、业务逻辑漏洞、第三方依赖内部的问题
- **常见形态**：模式匹配（快、误报多）→ 数据流/污点分析（准、需要更多上下文）

### DAST（Dynamic Application Security Testing，动态应用安全测试）

对**跑起来的服务**发 HTTP 请求，看响应。典型：ZAP、Burp。

- **看得到**：缺失的安全响应头、Cookie 标志、注入的实际可利用性、认证/会话问题
- **看不到**：没被访问到的代码路径（爬虫走不到 = 扫不到）、需要复杂业务状态才触发的问题
- **前提**：必须有一个**部署好的目标**。纯库、无 HTTP 服务的项目用不上
- **最大失效模式**：**没配认证 → 只扫到登录页 → 报告很干净**

### SCA（Software Composition Analysis，软件成分分析）

分析**依赖清单**，比对已知漏洞库。典型：Trivy、Dependency-Track、Snyk、Renovate（更新侧）。

- **看得到**：你用的第三方库有哪些已公开的 CVE
- **看不到**：你自己写的代码的问题；**以及这个 CVE 你到底触没触发**（见「可达性分析」）
- **前提**：需要**锁文件**。只有 `package.json` 没有 `package-lock.json` 时，Trivy 会直接跳过并报 "Supported files not found"

> ⚠️ **Trivy 常被叫成 SAST，它不是。** 它做的是 SCA + 配置检查（IaC misconfiguration）+ 密钥扫描。

### Secret Scanning（密钥扫描）

找误提交的凭据。典型：Gitleaks、TruffleHog、Trivy 的 secret scanner。

- **关键差异在扫描范围**：只扫工作区 vs **扫整个 git 历史**。密钥一旦提交，删掉文件再提交是没用的——历史里还在
- **进阶能力**：**有效性验证**（拿到疑似密钥去调一次 API，确认它是不是活的）。这能把误报砍掉大半，但意味着工具会向外发请求

### IAST / RASP（顺带一提）

- **IAST**：在应用里插桩，运行时观察数据流。介于 SAST 和 DAST 之间，准确率高但需要改运行时
- **RASP**：运行时自我保护，检测到攻击就阻断。是**防护**不是**测试**

这两个在中小团队很少见，了解即可。

---

## 二、漏洞标识与优先级

### CVE（Common Vulnerabilities and Exposures）

一个具体漏洞的**全球唯一编号**，如 `CVE-2019-10744`。它只是个 ID，不含严重程度。

### CWE（Common Weakness Enumeration）

漏洞的**类型分类**，如 `CWE-89`（SQL 注入）、`CWE-79`（XSS）。

**CVE 是"这一个洞"，CWE 是"这类洞"。** 一个 CWE 下有成千上万个 CVE。

### CVSS（Common Vulnerability Scoring System）

0–10 的**严重程度**评分。Critical / High / Medium / Low 就是它的分段。

> ⚠️ **CVSS 是严重程度，不是风险，更不是优先级。** 一个 CVSS 9.8 但你根本没调用到的函数，实际风险低于一个 CVSS 6.5 但正在被大规模利用的洞。只按 CVSS 排优先级会让人把时间花在错的地方。

### EPSS（Exploit Prediction Scoring System）

预测**未来 30 天被实际利用的概率**（0–1）。

这是对 CVSS 的补充：CVSS 说"如果被利用有多严重"，EPSS 说"多大可能被利用"。**两个一起看才是优先级。**

### KEV（Known Exploited Vulnerabilities）

CISA 维护的**已确认在野利用**清单。

上了 KEV 的洞不需要再讨论优先级——它已经在被真实攻击使用了。数据免费公开。

（「KEV 优先于其他信号」是常见做法，**本仓没有做过横向对比**，别当成实测结论。）

### NVD（National Vulnerability Database）

美国维护的漏洞数据库，多数工具的数据来源。注意它有**录入延迟**，某些生态（如 Go、Rust）有自己更快的库。

---

## 三、供应链

### SBOM（Software Bill of Materials，软件物料清单）

你的软件由哪些组件构成的**清单**。

> ⚠️ **SBOM 本身不是安全措施，它是库存台账。** 有 SBOM 不等于安全，它的价值在于**将来出事时能快速回答"我受影响吗"**。log4j 事件里，有 SBOM 的公司几小时内知道范围，没有的花了几周。

两种主流格式：
- **CycloneDX** —— OWASP 出品，偏安全场景，Trivy 能直接输出
- **SPDX** —— Linux 基金会出品，偏许可证合规场景

### 可达性分析（Reachability Analysis）

判断你的代码**是否真的调用到**了依赖中那段有漏洞的代码。

这是 SCA 分诊的关键能力：典型项目里**大部分依赖 CVE 是不可达的**——库里有洞，但你压根没用那个函数。没有可达性分析，你会在大量不可达的告警里消耗人力。

目前主要在商业产品里（Semgrep Supply Chain、Snyk 等）。

### Provenance / Attestation（来源证明 / 证明）

**Provenance**：这个制品是**谁、用什么源码、在什么环境下**构建出来的。
**Attestation**：对上述事实的**可验证签名声明**。

### SLSA（Supply-chain Levels for Software Artifacts）

供应链成熟度分级框架（L1–L3）。大致是：L1 有构建记录 → L2 有签名的来源证明 → L3 构建环境不可篡改。

### Sigstore / Cosign / Fulcio / Rekor

- **Cosign** —— 签名工具本身
- **Fulcio** —— 短期证书颁发机构（用你的 OIDC 身份换一张几分钟有效的证书）
- **Rekor** —— **公开的**透明日志，记录签名事实
- **keyless 签名** —— 不管理长期私钥，用 OIDC 身份 + Fulcio + Rekor

> ⚠️ **签名的价值全在验证那一步。** 没有任何环节强制校验签名，签名就只是给 CI 加时间的仪式。
> ⚠️ keyless 意味着**你的构建身份会进公开日志**（Rekor）。内部公司要先想清楚能否接受。

---

## 四、CI/CD

### Runner / Executor

执行 CI 任务的进程。**Runner** 是领任务的守护进程，**Executor** 是它用什么方式执行（shell / docker / LXC / k8s）。

### DinD（Docker-in-Docker）

在容器里跑一个**独立的 Docker 守护进程**，CI 任务用它而不是宿主的。

- **隔离了**：宿主上的其他容器和卷
- **没隔离**：同一个 dind 下的并发任务之间；**dind 容器自身必须 `privileged`**

> ⚠️ **DinD 不是沙箱。** 它防的是"横向碰到宿主其他服务"，不是"任务之间互相安全"。

### Ephemeral Runner（一次性 Runner）

跑完一个任务就销毁的 Runner。

- **隔离了**：跨任务的残留——token 复用、构建缓存投毒、遗留文件
- **成本**：不是一个开关。任务结束后 Runner 会**注销自己**，需要一个 supervisor 在每次任务前**重新注册**生成新凭据

**DinD 和 ephemeral 是正交的**：前者挡横向，后者挡纵向，通常两个都要。

### Artifact（制品）

流水线产出的文件：构建产物、测试报告、扫描结果。注意保留期——**扫描报告里可能有敏感信息**。

### Gate（门禁）

阻断合并或发布的检查点。

> ⚠️ **门禁的有效性和它的耗时成反比。** 慢到一定程度，人就开始想办法绕流程本身。
> 慢的检查放定时层，不放 PR 门禁（具体预算见 [05-architecture](05-architecture.md)）。

### Shift Left（左移）

把质量和安全检查往开发流程前段移。

> ⚠️ **被滥用成"把所有东西塞进 PR 门禁"。** 真正的左移是把**快的**检查移到本地（pre-commit 跑 gitleaks 是秒级），而不是把慢的检查堆在 PR 上。后者的结果是门禁被绕过。

---

## 五、代码审查与平台

> 本节都是**概念定义**。这些机制在 Forgejo 上**实际怎么表现**（状态码、谁能绕过、
> 哪个开关不管用）在 [20-ci-cd](20-ci-cd/README.md) §三，那里是唯一出处——
> 平台行为不可迁移，别把下面的定义当成任何平台的实际行为。

### Branch Protection（分支保护）

对特定分支的一组强制规则：禁止直推、要求审批数、要求状态检查通过等。

> ⚠️ **它约束的是有写权限的人。** 对仓库管理员，保护规则的强制力要另行验证——
> 「谁是管理员」往往比任何规则配置都更关键。

### Force Merge（强制合并）

绕过保护规则直接合并的动作。**它是合并接口上的一个独立能力，不一定受"对管理员也生效"
这类开关约束**——用之前实测。

### Required Approvals（必需审批数）

合并前需要的批准数量。

> ⚠️ **设成 0 时，"不允许自己审自己"等于不存在**——作者不能点 Approve，但可以直接合并。

### Stale Approval Dismissal（新提交后作废旧审批）

分支有新提交时自动撤销已有批准。

> ⚠️ **不开这个，必需审查形同虚设**：提干净 PR → 拿批准 → 再推任意代码 → 直接合并。

### CODEOWNERS

按路径指定负责人，改到这些路径时**自动请求**对应的人审查。

> ⚠️ 它是**自动请求审查**，不是"按路径提高审批数"。但别据此认为路径级强制审查做不到——
> 配合"拦未完成的正式审查请求"就能构成硬门禁。

### False Positive / False Negative（误报 / 漏报）

- **误报**：报了但不是问题 → 消耗信任，多了之后真问题也被忽略
- **漏报**：是问题但没报 → 危险，且**很难被发现**

**这两者是跷跷板**：调严阈值降漏报、升误报；调松反之。所以需要基线分诊而不是简单的"全开"或"全关"。

### Triage（分诊）

对告警逐条判断：真问题 / 误报 / 已接受风险，并定优先级。

> ⚠️ **换成商业工具消除不了分诊成本**，只是换了个界面。

### Baseline（基线）

当前已存在、暂时接受的告警集合。新接扫描器时先建基线，之后只对**新增**告警阻断。

**没有基线就切阻断，是门禁被绕过的头号原因。**

### Canary（哨兵）

一个**故意含已知问题**的样本，用来证明检测手段还活着。

**它的 CI 永远是红的，变绿的那天说明扫描器坏了。** 怎么建见
[30-security-scanning](30-security-scanning/README.md)；DAST 版哨兵见
[zap-dast](30-security-scanning/zap-dast.md)。

### 污点分析（Taint Analysis）

追踪不可信输入（source）如何流到危险操作（sink）。

- **单文件（intrafile）**：只在一个文件内追踪，开源工具常见
- **跨文件（interfile）**：跨函数、跨文件追踪，准确得多

各引擎到底支持到哪一层、哪些在付费墙后，见
[sast-engine-comparison](30-security-scanning/sast-engine-comparison.md)——
第三方文案在这一点上经常写错。

---

## 六、常见混淆（本文重点）

| 说法 | 问题在哪 |
|---|---|
| 「上了 SAST 就安全了」 | SAST 看不到运行时问题、依赖问题、业务逻辑漏洞。四类扫描各有盲区，不互相替代 |
| 「Trivy 是 SAST」 | 它是 SCA + 配置检查 + 密钥扫描。不读你的业务代码逻辑 |
| 「有 SBOM 了」 | SBOM 是台账不是防护。价值在出事时的响应速度 |
| 「CVSS 9.8，先修这个」 | CVSS 是严重程度不是风险。要结合 EPSS（利用概率）和 KEV（已在野利用），以及可达性 |
| 「扫描是绿的，没问题」 | **绿色的扫描器和坏掉的扫描器外观一致。** 见 [00-principles.md](00-principles.md) 第 1 条 |
| 「签名了所以安全」 | 没有强制验证的签名是仪式 |
| 「DAST 报告很干净」 | 先确认它**真的扫到了东西**——没配认证时它只看得到登录页 |
| 「配了必需审查」 | 审批数为 0、或不作废旧审批，两种情况下这条规则都等于不存在 |
| 「shift left，都放 PR 里」 | 慢门禁必被绕过。快的往左移，慢的放定时 |
| 「DinD 是沙箱」 | 它防横向不防纵向，且自身 privileged |
| 「上商业版就不用分诊了」 | 分诊成本换了个界面而已 |

---

## 七、合规（只解释名词）

- **SOC 2** —— 面向服务型企业的信任服务准则审计
- **ISO 27001** —— 信息安全管理体系认证
- **等保** —— 国内网络安全等级保护

**以上是背景定义，本仓没有做过合规项目，不要当实践经验读。** 唯一值得说的关联是：
这三者都涉及变更管理（谁改的、谁审的、有没有记录），而分支保护 + 必需审查天然产出
这类记录——**先把门禁做对，比事后补台账省事**。具体到某项认证要什么证据，问审计方。
