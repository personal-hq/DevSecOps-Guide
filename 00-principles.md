# 贯穿性原则

四条铁律，都是这次真踩出来的。技术会过时，这四条不会。

---

## 1. 绿色的扫描器什么都不说明

**一个从未报过任何问题的扫描器，和一个坏掉的扫描器，外观完全一致。**

必须用**已知坏样本**证明它会报。第一次配安全扫描时我们埋了三类问题，结果：

| 工具 | 首次结果 | 真相 |
|---|---|---|
| semgrep | ❌ 失败（4 处发现） | 工具正常，真的抓到了命令注入 |
| gitleaks | ✅ "no leaks found" | **样本不合格**：用了 `AKIAIOSFODNN7EXAMPLE`，那是 AWS 官方文档示例密钥，在 gitleaks 内置白名单里 |
| trivy | ✅ "Supported files not found" | **样本不合格**：只有 `package.json` 没有 lockfile，trivy 无法解析版本，直接跳过 |

两个"绿"都是假的。换成随机生成的假密钥 + 补上 lockfile 之后，三个全部正确报错。

**做法**：建一个故意含问题的样本仓库，让它的 CI **永远是红的**，并在 README 里
写明这是设计如此。它是扫描器的回归测试——哪天它变绿了，说明扫描器坏了。

## 2. 「错误消失」不等于「修好了」

必须找到一个**能直接观察到目标结果**的判据，而不是观察"没有报错"。

Renovate 的锁文件更新失败（artifactError）。换了自带包管理器的镜像后，报错消失、
日志显示 `"result": "done"` —— 看起来修好了。

**实际上锁文件根本没被改。** Renovate 对已存在的分支不会自动重算 artifact，
那次运行只是"没有新活可干"。真正的判据是 **PR 的文件列表里有没有 `package-lock.json`**。

## 3. 判断"被覆盖"要量，不要猜

CSS 调试时我连续猜错三次（假设是隐藏字段、假设是 Fomantic 规则、假设是解析错误），
每次都靠"看起来像"。正确做法是先分清两种情况，它们的修法完全不同：

```js
document.querySelectorAll('<你的选择器>').length
//  0  → 选择器根本没匹配上，改选择器
// >0  → 匹配上了但没生效，是特异度输了，用 !important 或提高特异度

getComputedStyle(el).<属性>   // 实际生效的值，不是你以为的值
```

真实案例：外链隐藏规则不生效。`querySelectorAll` 返回 1 —— 选择器是对的，
输在特异度：目标的父链是 `nav#navbar`，上游用 **ID 选择器 (1,0,0)** 设了
`display:flex`，压过本规则的 (0,2,2)。**这种情况下 `!important` 是正当手段**，
和"猜不出来所以加个 important 试试"有本质区别。

## 4. 工具缺失会伪装成事实

一个查询返回空，可能是"确实没有"，也可能是**你用来查的工具根本不在**。
后者会让你信心十足地报告一个不存在的问题。

这次两次踩中：

- **误报"备份丢了主题文件"** —— 我用 `unzip -l` 查备份内容，返回空。
  但宿主机**没装 unzip**。换 Python 的 `zipfile` 再查，条目全在 —— 自定义资源四项（主题 CSS / 字体 / logo / favicon）加上 `app.ini` 的两份副本，共六条命中。（只说「六个文件」会和运维文档里的「四项齐全」对不上，那是在数不同的东西。）
- **误判"设计画布离线不可用"** —— 无头 Chromium 渲染不出艺术板内容，
  file:// 和 http:// 都试了、等到 90 秒也一样。结论下了之后 owner 在真实浏览器里
  一开就正常。**是无头环境的限制，不是文件的问题。**

**做法**：查询返回"空"或"失败"时，先问一句——**我用来查的东西本身工作正常吗？**
最省事的验证是拿一个**已知会返回非空**的输入试一遍（同原则 1）。
