# 整体架构

自建 DevSecOps 不是把 gitleaks / trivy / semgrep / ZAP 都装一遍。工具是四条闭环里的节点，闭环本身、闭环放在哪一层、闭环失败时怎么办，才是架构决策。

## 准入标准

新工具进场前必须先答三个问题，答不上就不上：

1. **闭的是哪个环？**——发现之后走到哪一步算处理完，不是"扫出来"就结束。
2. **谁看输出？**——具体到人或角色，答不上这条的一律不上。没人看的告警是噪音，噪音的净效果是让人对下一条真告警也免疫。
3. **不上它，现在漏了什么？**——说不出具体缺口，说明这工具解决的是想象中的问题，不是当前的问题。

## 四条闭环

不按工具清单组织，按闭环组织——同一个工具放错闭环位置，等于没做。

### 1. 密钥泄露闭环

pre-commit（gitleaks）→ CI gitleaks（`--redact`，扫全历史）→ **泄露后轮换 SOP**

最常被跳过的是第三段。密钥一旦推上远端，删 commit 没有意义——它已经在 git 历史、CI 日志、别人拉过的克隆里。**唯一正确响应是轮换**，不是清历史。

### 2. 依赖漏洞闭环

trivy 发现 → Renovate 提 PR → CI 验证 → 合并

**没有 Renovate 这条链就断在告警堆积**：trivy 只负责发现，升级到哪个版本、锁文件怎么改不是扫描器的工作。少了这一环，trivy 的输出会变成一份没人处理的清单。

### 3. 代码缺陷闭环

semgrep → PR 评论 → 修 or 豁免（有到期日）

**闭环的终点是"这条发现被处理掉了"，不是"闸变红了"。** 阻断只是让人不得不处理的
一种手段，不是闭环本身——一道没人修、全靠豁免绕过的红闸，闭环照样是断的。

规则集要裁剪，不是拿全量规则直接上——原因见下面"最容易失败的三处"第一条。
**Semgrep `error` 什么时候从"评论"升级成"阻断"，判据见公司方案，不是默认就该阻断。**

### 4. 运行时闭环

部署 staging → ZAP → 报告

细节见 [30-security-scanning/zap-dast.md](30-security-scanning/zap-dast.md)。

## 分层：什么放哪里

这是最重要的架构决策——同一个工具放在不同层，产生的效果完全不同。

| 层 | 内容 | 时限 | 阻断 |
|---|---|---|---|
| 本地 pre-commit | 格式化、gitleaks、快速 lint | 秒级 | 是 |
| PR CI · 硬门 | gitleaks、trivy（lockfile）高危、单测 | ≤5 分钟（本行与下行**合计**） | 是 |
| PR CI · 建议 | semgrep | 同上 | **默认否**（毕业后可改，判据见方案） |
| 定时 | trivy 全量重扫、ZAP、SBOM | 小时级 | 否，出工单 |
| 平台 | 分支保护、必需审查、Runner 隔离 | — | 是 |

铁律：**慢的东西不进 PR 门禁**。超过 5 分钟，人就开始想办法绕过流程本身——拆小改动躲检查、找理由跳过 hook、催人加速审批。

定时层的独有价值不是"更彻底"，是**时间维度**：漏洞库会更新，今天扫描干净的代码下周可能被曝出新 CVE。已经合并、CI 早就跑绿的代码需要被重新扫描——这是 PR 门禁原理上做不到的事，它只在合并那一刻跑一次。

## 最容易失败的三处

1. **门禁噪音 → 全员绕过。** 新扫描器不要一上来就设成阻断：先 warn-only，收集基线，把已知历史问题标 ignore，确认噪音可控再说。反过来做——一上线就红——团队学会的是跳过它，不是修它。

   > **"两周后切阻断"是我们这两个仓的选择，不是通用规定。** 能切是因为规则集
   > 已经裁到噪音可控；那是实例状态。误报率的关键差别在于：Gitleaks 和 Trivy
   > 的发现是二元的，Semgrep `error` 的误报率**完全取决于你自己的规则集**——
   > 陌生团队拿开箱规则直接阻断，三周后那道闸就被关了。
   > **一道闸什么时候从"建议"升到"阻断"，判据见公司方案，不是照抄这里的两周。**
2. **扫描器静默失效。** 绿色不代表干净，也可能代表坏了（见 [00-principles.md](00-principles.md) 第一条）。必须有一个故意留问题的 canary 仓库，它的 CI 应该永远是红的。我们在实例上单开了 `security-scan` 当这个用途（**不在本指南仓里**，要自己建一个），它变绿等于扫描器坏了。
3. **有告警没 owner。** 每条规则上线前必须回答：谁负责、多久响应、修不了怎么豁免。答不上的不要上线——这和准入标准第二问是同一件事，在规则粒度上再问一遍。

## 分阶段实施

按投入产出比排序，不是按技术难度：

| 阶段 | 内容 | 投入 | 防住什么 |
|---|---|---|---|
| 0 | 分支保护 + 必需审查 + gitleaks（pre-commit+CI） | 1 天 | 最贵的事故 |
| 1 | trivy + Renovate 闭环（见下方注） | 3 天 | 漏洞长期挂着 |
| 2 | semgrep（先裁剪规则集，再 warn-only 观察） | 1 周 | 代码层缺陷 |
| 3 | ZAP + 定时全量重扫 | 视部署链 | 运行时 |

> **注：Renovate 不是合入门禁的前提。** 没有它，Trivy 照样能挡（手工升依赖一直可行），
缺的是**修复路径的自动化**——发现会堆成没人处理的清单。所以它是**闭环缺口**，
不是门禁没装。人力紧张时先砍它是对的；本仓把它放 Phase 1 是因为我们有余量。

**Phase 0 的投入产出比甩开后面几个阶段几个数量级**——1 天投入挡的是密钥或恶意提交直接进主干这类最贵的事故，后面三个阶段修的都是可以晚一点发现、晚一点修的问题。

## 现在不建议做的

- **完整 SBOM / 供应链签名**——无合规要求前是负担，不是防护
- **自建 semgrep 规则库**——先用社区规则跑半年，再谈定制
- **ZAP full scan 进 PR 门禁**——违反"慢的东西不进 PR 门禁"，而且它会真的发起攻击（细节见 [zap-dast.md](30-security-scanning/zap-dast.md)）
- **镜像签名与准入控制**——除非已经在 k8s 跑生产
